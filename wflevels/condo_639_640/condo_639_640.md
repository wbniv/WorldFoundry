# condo_639_640 — 205/639 + 205/640 air‑con walkthrough

A doll‑house walkthrough of the two condo units and the planned split‑system installation,
converted from the Blender study `~/docs/aircon/units-639-640.blend` (generated by
`~/scripts/aircon-blender.py`; rebuild the model with `cd ~ && task aircon-blender`).
The model is an outline‑and‑partitions study, not a survey: interior walls follow the doc's
schematic rooms; front‑door positions and the 2.4 m drop ceiling are assumptions.

Plan and mapping decisions: [docs/plans/2026-09-19-condo-639-640-level.md](../../docs/plans/2026-09-19-condo-639-640-level.md).

## Build & run

```bash
task condo-level          # Blender export (needs the wf_blender add-on) + build-level
task run-condo            # WF_RECORD=1 / WF_FULLSCREEN=1 as usual
CONDO_BLEND=/other/units-639-640.blend task condo-level   # alternate source model
CONDO_SPAWN=2.0,-11.0,0.3 blender --background --python wflevels/condo_639_640/blender_create_condo.py
                          # spawn override for screenshots (639 kitchen); then task build-level -- condo_639_640
```

Re‑running `task condo-level` is a no‑op until the `.blend`, the script or the standalone
wrapper changes.

## Room tour video

`task video-condo-639` → [`tour-639.mp4`](tour-639.mp4) (30 s, 640 × 480, burnt‑in room captions;
`tour-639.srt` alongside). The tour build `wflevels/condo_639_640_tour` is the same level with the
player's Forth replaced by a waypoint servo compiled from [`tour-639.path.json`](tour-639.path.json)
(`task tour-condo-639`); `tests/record_condo_639_tour.py` records it and post‑processes with ffmpeg.
Route: front door → kitchen → bath‑S → project room → guest bedroom → back bath → recessed patio →
patio → party‑wall connector → 640 room‑2.9×3.3 → master bedroom → closet → master bath.
Plan: [docs/plans/2026-09-19-condo-639-tour-video.md](../../docs/plans/2026-09-19-condo-639-tour-video.md).

## Controls (doom‑stick, `Turn Rate = 0`)

| Input | Effect |
|---|---|
| ▲ / ▼ | walk +Y (toward the back / balconies) / −Y (toward the front doors) |
| ◀ / ▶ | strafe −X / +X (screen left / right) |
| A (Space) | small hop |

Spawn: just inside 639's front door at `(4.66, −14.5, 0.3)` (relative to the unit floor), facing +Y. Walking pace ≈1.5 m/s
(`Running Acceleration` 40; ground speed ≈ accel/26 — `CONDO_ACCEL=` overrides).
Camera `cs_dollhouse`: relative offset `(0, −3.5, 9)` from the player, looking at the chest.

## What maps to what

| Blender | WF actor | Notes |
|---|---|---|
| `unit-639`, `unit-640` | `statplat` mesh, same name | shell = floor slab + exterior walls, openings cut in; Jolt trimesh |
| `<unit>-<name>-wall`, `…-jambN`, `…-door-header`, `….001` | `statplat` mesh, same name | rotation + scale baked into the mesh |
| `640-window-0`, `639-window-1`, `640-curve-window-1..6` | `statplat` mesh, same name | opaque light‑blue glass, thickened 0.02 → 0.10 m |
| `*-indoor`, `*-outdoor`, `*-consumer-unit`, `640-midea-*-A-closet-wall` | `statplat` mesh, same name | props |
| `*-power`, `*-lineset`, `*-drain` (curves) | `statplat` mesh, same name | bevel → tube mesh; red / copper / light blue |
| room outlines (display WIRE: `639-kitchen`, `640-bath`, …) | `target`, same name | bbox = room; invisible locator, no collision |
| — | `corridor`, `corridor-parapet` | 18 × 2 m open‑air corridor slab outside both front doors at unit floor level, 1.1 m railing on its outer edge |
| — | `site-map` | ±80 m textured ground quad at world z = 0 — `condo_ground.tga`, the OSM flat map around the pin |
| — | `skydome` | inverted 160 m sphere at the 6th‑floor viewpoint — `condo_sky.tga`, equirect panorama (sky, sun, skyline silhouettes, projected ground); `Mass 0` so the camera never "collides" with it |
| — | `zone-interior`, `zone-balcony`, `cs_balcony`, `BalconyLook` | balcony POV camera + its pan (below) |
| — | `Player`, `CamTarget`, `LookAt`, `cs_dollhouse`, `Camera`, `Sun`, `AmbientLight`, `Matte`, `Level`, `Director` | infrastructure |
| both units | `room_condo` | the **single** WF room; condo rooms are the `target`s above |

Mesh files are `<slug>.iff` (lowercase, non‑alnum → `_`, ≤ 30 chars — long names are cut to
21 chars + a 4‑hex CRC), while the actor `NAME` in the `.lev` keeps the Blender name verbatim.

## Site — where the units really are

The units are on the **6th floor** of a condo at
[34 Soi Sathu Pradit, Bang Phong Phang, Yan Nawa, Bangkok 10120](https://maps.app.goo.gl/CTkxPDsuX7umeZwX8)
(13.6935894 N, 100.5334137 E), back (balconies) facing straight **west**. `site_constants.py`
holds the pin, the compass mapping and the elevation; `make_site_textures.py` turns a committed
OpenStreetMap extract (`site-osm.json`, ODbL; `task condo-osm-fetch` refreshes it) into the two
textures (`task condo-textures`, offline and byte‑stable).

| Compass | Level axis | What's there |
|---|---|---|
| West | +Y | balconies, the Chao Phraya ≈ 1.6 km out |
| East | −Y | front doors, corridor, Sathu Pradit Road 11 m from the pin |
| North | +X | unit 639 is north of 640 |
| South | −X | |

Elevation: `UNIT_Z = (6 − 1) × (3.0 m clear + 0.15 m slab) = 15.75 m` — everything is authored
with the unit floor at z = 0 and lifted in one pass at the end of the script, so the tour path,
`darken_floors` and the slab outline never see the offset. The map pin geocodes to the Soi 34 /
Sathu Pradit Road corner (a road in OSM, not a footprint): `SITE_OFFSET_EN` in
`site_constants.py` slides the level origin east/north once the real building is identified.

Textures ride the PERM atlas at 1024² (`textile.flags`), which needs the `--vram-*` flags
`run-condo` and the tour recorder pass. `CONDO_CAM=x,y,z` / `CONDO_LOOK=x,y,z` override the
doll‑house camera/aim offsets for screenshots. Plan:
[docs/plans/2026-09-19-condo-site-skybox.md](../../docs/plans/2026-09-19-condo-site-skybox.md).

## Balcony camera — step onto 639's patio and you're looking at Bangkok

The doll‑house shot pitches 69° down, so the sky is never in its frame. Two `ActBoxOR` zones
switch shots: `zone-interior` (both units + corridor) → `cs_dollhouse`, `zone-balcony`
(the `639-patio` + `639-patio-recessed` outlines) → `cs_balcony`, the player's POV — a
`Relative` offset (0, 1.5, 1.7): eye height, 1.5 m ahead, which on the patio is just past
the pony wall in free air. While you stand there the look target (`BalconyLook`, a scripted
platform like the moon's `launch_tracker`) pans over 5 s from the street to the south‑west up
to the towers to the north‑west, timed from mailbox 97 (the Director records the level time
you stepped on, zeroes it when you leave). Walk back inside and the doll‑house returns.

Facts this depended on (the CamShot docs are being corrected — see TODO):

- `movecam.cc SetCameraParametersFromShot`: `Relative` → position = (camshot − Follow) +
  Track Object; `Absolute` → the camshot's own point; direction = Target − camshot, world
  space. `Fixed` only means "don't add the tracked object's heading".
- The camera is a physics body: whenever its bbox overlaps any actor with `Mass > 0`
  (`Actor::CanCollide`) it stops seeking Z and **climbs** (`BungeeCameraHandler` ClimbRate) —
  so a POV camera inside a room sails upward, and the sky dome must have `Mass 0` or its
  world‑sized bbox counts as a permanent collision.
- In bungee mode (`FLAG … 1` in the standalone wrapper) `INDEXOF_CAMSHOT` is re‑read every
  tick and never cleared: the last index written wins; an `ActBoxOR` writes its mailbox every
  tick the player overlaps it and never clears it, so the Director forwards and zeroes.

## Files

`blender_create_condo.py` (conversion), `site_constants.py` + `make_site_textures.py` + `site-osm.json` → `condo_sky.tga`, `condo_ground.tga` (site skybox), `textile.flags` (1024² PERM page), `condo_639_640.blend` (assembled WF scene, `room_condo` hidden), `condo_639_640.lev` + `*.iff` meshes (export), `condo_639_640-standalone.iff.txt` (L4 wrapper), `tests/walk_condo.py` (bridge-driven collision check).

## Presentation tweaks

- Floor‑top faces use the unit colour × 0.55 (`CONDO_FLOOR_SHADE`) so walls and doorways read from
  the overhead camera; see [the plan](../../docs/plans/2026-09-19-condo-darker-floors.md).

## Not mapped

- **Floors 1–5 / the building itself** — not modelled; from the doll‑house camera the ground map
  shows flat under the unit edges. Needs the building's footprint pinned first (`SITE_OFFSET_EN`).
- **Glass transparency** — `MATL` has no alpha; panes are opaque light blue.
- **`unit-640-drop-ceiling`** — hidden in the source and it would hide 640's interior from
  the overhead camera.
- **`640-midea-option-B-over-door`** — rejected alternative, hidden collection, never appended.
- **Consumer‑unit material** — none in the source; given a light‑grey flat colour.
- **Tube end caps** — bevelled curves export as open tubes (invisible at 4–6 cm radius).
- One sub‑threshold triangle (a bevel corner on `639-daikin-power`) is dropped so the engine's
  face‑normal assert doesn't fire; see the troubleshooting doc.
