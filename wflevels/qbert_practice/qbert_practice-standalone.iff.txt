// qbert_practice-standalone.iff.txt — L4-wrapped form of qbert_practice for wf_game -L.
//
// Build (via the standard pipeline, which emits both inner and standalone):
//   bash wftools/wf_blender/build_level_binary.sh qbert_practice
//
// Or directly:
//   ../../wftools/iffcomp-rs/target/release/iffcomp -binary \
//     -o=../qbert_practice-standalone.iff qbert_practice-standalone.iff.txt
//
// Run:
//   task run-level -- wflevels/qbert_practice-standalone.iff
//
// Phase 1 (cube consolidation, 2026-05-10) dropped the inner iff from
// 1.93 MB to ~45 KB; pool budgets below are sized for the 28-cube design
// with snowgoons-class margin.
{ 'L4'
    { 'ALGN' .align( 2048 ) }
    { 'RAM'
        'OBJD' 200000l                  // ~45 actors total post-Phase-1; matches snowgoons
        'PERM' 500000l
        'ROOM' 500000l                  // 28 cube actors, all sharing one cube.iff (~1.1 KB)
        'FLAG' 1l 1l                    // doomstick, bungeecam
    }
    { 'ALGN' .align( 2048 ) }
    [ "../qbert_practice.iff" ]
}
