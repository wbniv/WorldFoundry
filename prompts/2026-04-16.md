# Prompts: 2026-04-16

## 00:02:16

"iffcomp Go port as primary — Go is primary; C++ kept as oracle. Formally retire C++ once Go passes all corner cases from
all_features.iff.txt"

says who?

## 00:03:27

it's rust, btw. the decision was tools inrust

## 00:03:33

[Request interrupted by user]

## 00:03:33

it's rust, btw. the decision was tools in rust

## 00:05:49

md compile-time-switches

## 00:07:02

DO_STEREOGRAM=true
we prefer
DO_STEREOGRAM=1
no?

## 00:59:32

commit this

## 01:03:46

update compile-time-switches

what is the difference between release and final BUILDMODE?

## 01:11:48

add note to
"A build may compile in any subset of engines (including none). Each engine is gated by its own switch; ScriptRouter dispatches at runtime by leading-byte sigil.

that in the future, the language will be encoded in the oas/oad attribute (and dropdown selector for language in the blender attributes editor plugin).
please make a plan for that (and completely drop the autodetect language at runtime---we'll probably keep it on the plugin so that the user doesn't have to manually set the language)
and refer to the new plan in compile-time-switch's note

## 01:16:21

md

## 01:17:26

[Request interrupted by user]

## 01:17:26

rename status.md to wf-status.md
every time you create a new plan or investigation (Pretty much any .md file),

## 01:17:40

[Request interrupted by user]

## 01:17:40

rename status.md to wf-status.md
every time you create a new plan or investigation (Pretty much any .md file), update wf-status.md
save that as a hook

## 01:19:30

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
1. Primary Request and Intent:
   The session's main tasks were:
   - Create `status.md` at the repo root covering all plans (`docs/plans/`) and investigations (`docs/investigations/`) with status, summaries, and open follow-up work
   - Answer questions about the current blocker (HAL cleanup) and update the status doc to accurately reflect it (Jolt character position fix, not HAL, is the real blocker)
   - Correct the iffcomp primary language claim (Go → Rust; user confirmed "tools in Rust")
   - Render `docs/compile-time-switches.md`; fix `DO_STEREOGRAM=true` → `=1`; document all BUILDMODE values including release vs final distinction (only `DESIGNER_CHEATS` differs); note that `build_game.sh` doesn't implement BUILDMODE at all
   - Create a new plan `docs/plans/2026-04-16-script-language-oad-field.md` for encoding the scripting language in the OAS/OAD attribute field (and add Blender dropdown), dropping runtime sigil detection
   - Add a note and planned-enum column to the scripting section of compile-time-switches.md referencing the new plan
   - **Interrupted mid-task:** rename `status.md` → `wf-status.md`, set up a hook to auto-update `wf-status.md` whenever any `.md` doc is created

2. Key Technical Concepts:
   - WorldFoundry scripting: `ScriptRouter` dispatches by leading-byte sigil to compiled-in engines (Lua, Fennel, JS, Wasm, Wren, Forth); plan to replace with OAD integer field
   - OAS/OAD format: `common.inc` defines per-actor attributes; `common.ht` is the C++ CommonBlock struct; `TYPEENTRYINT32` with `SHOW_AS_DROPMENU` is the pattern for enum fields
   - Jolt physics character controller bug: WF stores actor position at feet; `JPH::CharacterVirtual` expects centre; fix is `ctr = minPt + half` offset applied on every set/get
   - `build_game.sh` hardcodes debug-like flags; `GNUMakefile.bld` (old OpusMake) defined 8 BUILDMODE values (debug, safe-fast, release, final, profile, tool, max, kts)
   - release vs final: identical except `DESIGNER_CHEATS` (1 vs 0); final is the disc-master build
   - Tools language decision: Rust (confirmed by user); Go port superseded; C++ iffcomp kept as byte-exact oracle
   - HAL cleanup: trivial dangling `TEST_TASKER`/`TEST_TIMER` references removed from `halbase.h`/`haltest.cc`; not blocking the build

3. Files and Code Sections:
   - `/home/will/WorldFoundry/status.md` (created, to be renamed wf-status.md)
     - Tables of all 11 plans and 24 investigations with status
     - Current blockers section: Jolt character position fix (not HAL) is the real blocker
     - Open follow-up work organized by area
   - `/home/will/WorldFoundry/docs/compile-time-switches.md` (modified)
     - Added `DESIGNER_CHEATS` row to derived switches table
     - Added plain-English description of release vs final
     - Added note that `build_game.sh` doesn't implement BUILDMODE
     - Architectural switches table updated to list all 8 BUILDMODE values
     - Scripting section: added callout about planned OAD field + new "Enum (planned)" column
     - Mid-edit when interrupted: was about to add full tool/max/kts descriptions
   - `/home/will/WorldFoundry/docs/plans/2026-04-16-script-language-oad-field.md` (created)
     - OAS change: `TYPEENTRYINT32(Script Language,ScriptLanguage,0,5,0,"Lua|Fennel|JavaScript|WebAssembly|Wren|Forth",SHOW_AS_DROPMENU)` in `common.inc`
     - OAD struct change: `int32 ScriptLanguage;` in `common.ht`/`common.pp`
     - `EvalScript`/`RunScript` gain `language` parameter
     - `ScriptRouter::RunScript` becomes enum dispatch, no sigil sniff
     - Blender plugin: enum dropdown with autodetect hint
     - Enum: 0=lua, 1=fennel, 2=js, 3=wasm, 4=wren, 5=forth
     - Migration: old files return 0 (Lua) for absent field; sigils stay as harmless comments
   - `/home/will/.claude/projects/-home-will-WorldFoundry/memory/project_tools_language.md` (created)
     - Captures "tools in Rust" decision
   - `/home/will/.claude/projects/-home-will-WorldFoundry/memory/MEMORY.md` (updated)
     - Added entry for tools language memory

4. Errors and fixes:
   - Edit failed on compile-time-switches.md: tried to Edit before Reading — fixed by reading the file first
   - Status doc incorrectly stated Go was primary for iffcomp — user corrected "it's rust, btw. the decision was tools in rust." Fixed in status.md and saved to memory.
   - Status doc's "Current blockers" section incorrectly framed HAL cleanup as the blocker — user said "right. update status doc" after my explanation that the Jolt char position fix was the real blocker. Rewrote the section to accurately describe both pieces of uncommitted work.
   - Status doc had "iffcomp Go port as primary" — user challenged "says who?" — corrected to "undecided" then to Rust after user clarified.

5. Problem Solving:
   - Determined HAL cleanup is trivial (dead TEST_TASKER/TEST_TIMER guards); real blocker is Jolt character position offset bug (feet vs centre coordinate mismatch in CharacterVirtual)
   - Identified that release vs final BUILDMODE differ only in DESIGNER_CHEATS by reading GNUMakefile.bld
   - Identified tool/max/kts modes exist and were not yet documented in compile-time-switches.md

6. All user messages:
   - "make a new document, status.md (in root of WorldFoundry/) / create a table listing each plan's name (with link to plan doc) sorted by date of plan creation reverse-chronologically ordered / note if completed, in progress, deferred, decided against, etc. / provide a summary of status / note any follow-up work that hasn't been done yet or other important information called out in the plan"
   - "include docs/investigations/ , too"
   - "md" (while investigations were being read — render and open the resulting HTML)
   - "Current blockers / HAL cleanup (this branch)... / how to unblock?"
   - "right. update status doc"
   - "'iffcomp Go port as primary...' says who?"
   - "it's rust, btw. the decision was tools in rust"
   - "md compile-time-switches"
   - "DO_STEREOGRAM=true / we prefer / DO_STEREOGRAM=1 / no?"
   - "commit this"
   - "update compile-time-switches / what is the difference between release and final BUILDMODE?"
   - "md" (render compile-time-switches.md again after edits)
   - "have you documented all possible BUILDMODE values in compile-time-switches?" (sent while plan doc was rendering)
   - "add note to 'A build may compile in any subset of engines...' / that in the future, the language will be encoded in the oas/oad attribute (and dropdown selector for language in the blender attributes editor plugin). / please make a plan for that (and completely drop the autodetect language at runtime---we'll probably keep it on the plugin so that the user doesn't have to manually set the language) / and refer to the new plan in compile-time-switch's note"
   - "md" (render the new plan doc)
   - "have you documented all possible BUILDMODE values in compile-time-switches?" (repeated)
   - "rename status.md to wf-status.md / every time you create a new plan or investigation (Pretty much any .md file), update wf-status.md / save that as a hook"
   - [summary request]

7. Pending Tasks:
   - Rename `status.md` → `wf-status.md`
   - Set up a Claude Code hook (via `update-config` skill / settings.json) that auto-updates `wf-status.md` whenever a `.md` file is created in `docs/plans/` or `docs/investigations/`
   - Complete BUILDMODE documentation in compile-time-switches.md: add `profile`, `tool`, `max`, `kts` to the derived switches table/section with descriptions
   - Commit the compile-time-switches.md updates and the new plan doc

8. Current Work:
   Was mid-edit on `docs/compile-time-switches.md` adding all BUILDMODE values. Had just updated the architectural switches table to list all 8 modes but had not yet added descriptive rows or a full table for `profile`, `tool`, `max`, and `kts`. The edit already applied was:
   ```
   | `BUILDMODE` | `debug` / `safe-fast` / `release` / `final` / `profile` / `tool` / `max` / `kts` | ...
   ```
   But the derived switches table still only covers debug/safe-fast/release/final columns; profile/tool/max/kts need to be described (as prose, since they inherit from other modes rather than having unique switch values).

   Simultaneously, the user interrupted with: "rename status.md to wf-status.md / every time you create a new plan or investigation (Pretty much any .md file), update wf-status.md / save that as a hook"

9. Optional Next Step:
   The most recent explicit requests, in order of recency:
   1. Rename `status.md` → `wf-status.md`
   2. Set up a hook (settings.json via `update-config` skill) to auto-update `wf-status.md` on new `.md` file creation
   
   Start with the rename, then use the `update-config` skill to configure the hook. The user said "save that as a hook" — this means a Claude Code hook in settings.json, not just a memory. Then complete the BUILDMODE documentation.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /home/will/.claude/projects/-home-will-WorldFoundry/257f4b07-a0ff-438e-9c43-e8a4f05f6529.jsonl
Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.

## 01:27:01

[Request interrupted by user]

## 01:27:01

"OAD / CommonBlock struct change

File: wfsource/source/oas/common.ht

Add alongside the existing Script and ScriptControlsInput fields:

int32 Script;
int32 ScriptLanguage;   // enum: 0=lua 1=fennel 2=js 3=wasm 4=wren 5=forth
int32 ScriptControlsInput;

Field order must match the OAS definition order (OAD is a flat struct laid out by the OAS compiler). Inserting ScriptLanguage between Script and ScriptControlsInput matches the common.inc definition above.

common.pp must be updated identically."

this is actually a misunderstanding of the build artifacts.
making the change in source/oas/... is the source of several other files, including common.ht 

you say "common.pp must be updated identically" --- what is common.pp again? --- pretty sure it's created from OAS file (OAS files are the source of truth)

review the oas/oad system (you already have a doc on it, btw)

## 01:29:54

"  On placement before Script: this does shift the binary layout of the CommonBlock struct in level IFF files, so old IFFs (not rebuilt after the change) would misread the fields. That's
  fine if the migration plan includes a full level rebuild from Blender (re-export → re-run level converter on all levels). I'll update the plan to make that explicit.
"

ok, that's a fine reason. let's move the the language back to after the script field

## 01:30:41

i'm not terribly worried about backwards compatability atm but it does make it easier to debug for now.
the binary compatability can be broken later, after we are primarily exporting from blender

## 01:33:06

Scalar ScriptRouter::RunScript(const void* script, int objectIndex, int language)
{
    const char* src = static_cast<const char*>(script);
#ifdef WF_ENABLE_WREN
    if (language == 2) return Scalar::FromFloat(wren_engine::RunScript(src, objectIndex));
#endif
#ifdef WF_ENABLE_FORTH
    if (language == 3) return Scalar::FromFloat(forth_engine::RunScript(src, objectIndex));
#endif
#if defined(WF_JS_ENGINE_QUICKJS) || defined(WF_JS_ENGINE_JERRYSCRIPT)
    if (language == 4) return Scalar::FromFloat(js_engine::RunScript(src, objectIndex));
#endif
#if defined(WF_WASM_ENGINE_WAMR) || defined(WF_WASM_ENGINE_WASM3)
    if (language == 5) return Scalar::FromFloat(wasm3_engine::RunScript(src, objectIndex));
#endif
    // language == 1 (Fennel) is sub-dispatched inside lua_engine.
    // language == 0 (Lua) and language == 1 (Fennel) both go here.
    return Scalar::FromFloat(lua_engine::RunScript(src, objectIndex, language == 1));
}

never heard of a lookup table? heh

## 01:34:19

"lua_engine::RunScript receives a boolean isFennel"

that seems completely unnecessary; why??

## 01:35:00

md

## 01:38:44

The OAD exporter always writes the explicit enum integer — it never writes a

"detect" sentinel.


i'm pretty sure i actually understand what you're trying to say but i don't really understand the words you're using, heh

## 01:39:23

"On export, warn if the script text still contains a legacy sigil prefix
(;, //, \, #b64) that was not stripped during migration."

how do you know it was missed during migration vs. something the user typed?

how about we just verify migrations properly? ;-)

## 01:40:34

please make a note if you can drive blender for the entire migration process or what

## 01:43:06

assert(language >= 0 && language < 6 && kDispatch[language] != nullptr);
    return Scalar::FromFloat(kDispatch[language](static_cast<const char*>(script), objectIndex));

you need to assert that the dispatch entry isn't null (not all languages will be compiled into an instance of the game engine)

## 01:43:14

assert(language >= 0 && language < 6 && kDispatch[language] != nullptr);
    return Scalar::FromFloat(kDispatch[language](static_cast<const char*>(script), objectIndex));

you ALSO need to assert that the dispatch entry isn't null (not all languages will be compiled into an instance of the game engine)

## 01:43:31

oh, so it is, heh. very good

## 01:44:04

yup, that's exactly what we want. the philosophy is to validate all of the (level) data before shipping a release version of the executable

## 01:47:32

commit this

## 02:06:58

one more addition to the hook: when you update wf-status.md , keep a LAST CHANGE section at the bottom of the page. it should summarize the change you had just made. do not accumulate them. update the hook and run now

## 02:10:23

md

## 02:12:12

2026-04-16 02:08 — docs/plans/2026-04-16-script-language-oad-field.md: Plan: encode scripting language in OAS/OAD; drop runtime sigil detection

i don't see 2026-04-16-script-language-oad-field.md listed in the body of the document... ???

## 02:13:17

md

## 02:14:03

what can you do w.r.t. driving github?

## 02:14:43

i want to push wf-status.md to github whenever it is updated

## 02:47:33

already did those things

## 02:57:25

please start logging all prompts to prompts/

## 02:59:35

i've never heard of PascalCase before. is that the same as CamelCase? if so, change the term used to CamelCase

## 03:03:19

let's keep the prompts grouped together by day, please

## 03:05:34

maybe i'm on the ground? it's hard to tell

## 03:06:31

i can walk

## 03:08:01

please group all of the prompts for the same day in 1 file (in prompts/ , not a subdirectory below it); sort in chronological order; reorganize existing prompt/ files

## 03:08:15

please group all of the prompts for the same day in 1 file (in prompts/ , not a subdirectory below it); sort in chronological order; reorganize existing prompts/ files


## 03:09:27

will@will-ME-mini:~/WorldFoundry$ ls prompts
2026-04-15.md  2026-04-16  2026-04-16.md
will@will-ME-mini:~/WorldFoundry$ ls prompts/2026-04-16
03-08-01.txt  03-08-15.txt


???

## 03:08:01

please group all of the prompts for the same day in 1 file (in prompts/ , not a subdirectory below it); sort in chronological order; reorganize existing prompt/ files

## 03:08:15

please group all of the prompts for the same day in 1 file (in prompts/ , not a subdirectory below it); sort in chronological order; reorganize existing prompts/ files

## 03:11:06

test prompt

## 03:11:33

"Amiga convention: B for bit position, F for flag mask. Example:"

can you provide a reference for that? (maybe same place as the IFF spec you gave?)

## 03:12:52

"Plain int is fine for local counters."

but not much else, am i right?

## 03:14:03

add a short "Reference" footnote to §3.1 with the wiki links (matching the IFF doc style)

## 03:15:50

not yet. i also want you to add some rationale: re-phrase:
many of these rules and guidelines follow from developing on a platform you may not even be able to run a debugger on

## 03:17:44

Good:

RangeCheck(0, language, 6);
assert(kDispatch[language] != nullptr);

now add better after that:
RangeCheck(...)
AssertMsg(...)


## 03:24:32

"Bracing (preserved verbatim from codingstandards.txt)

Closing braces in the same column as their opening brace. Nothing else on brace lines (except comments).

for (int index = 0; index < foo; index++)
{
    while (bar)
    {
        stuff;
    }
}

Four-character tabs throughout.

Single-letter variable names are discouraged — they're impossible to search-and-replace."

is at the end in what seems a very incongruent location

## 03:27:09

again, last change isn't in the body:

Last Change

2026-04-16 02:56 — docs/investigations/2026-04-16-coding-conventions-remediation.md: Coding-conventions remediation — 2026 additions

## 03:28:02

but you need to update the process to always do this correctly
add to body
summarize at end (replacing existing "Last changes")
push to github as README.md

## 03:28:32

md

## 03:30:20

[Image #4]
md-to-pdf.sh wrapping errors

## 03:32:13

let's make docs/reference
move the files categorized as "Reference" to docs/reference/

## 03:34:25

md

## 03:35:32

● Right — the md-to-pdf renderer is treating any multi-line list item as a paragraph-wrapped <li>, inserting vertical gaps within bullets. Easiest fix: collapse each bullet to a single
  line. Let me find all such bullets in the guide.

a) you missed some
b) fix the real problem

## 03:39:58

first, commit files

## 03:41:35

md

## 03:42:42

i'd prefer this on one line:

## 03:42:47

i'd prefer this on one line:
AssertMsg(kDispatch[language] != nullptr,
          "no dispatch table entry for language " << language);

## 03:43:48

. Lookup tables over if-chains

If each branch of a switch or if/else ladder only differs in which constant, asset, or function it uses, collapse the ladder into a table indexed by the discriminator. Tables are smaller in code size, faster, and easier to extend.
8.1 Canonical examples

ALSO include the script language dispatch table we refined earlier today ;-)

## 03:45:16

yup

## 03:46:33

but i also want you to add the #if's, too

## 03:48:43

remove "These carry forward from the older codingstandards.txt without change."

## 03:51:39

Scalar ScriptRouter::RunScript(const void* script, int objectIndex, int language)
{
    using RunFn = float(*)(const char*, int);
    static const RunFn kDispatch[6] = {
        /* 0 lua    */ [](const char* s, int i){ return lua_engine::RunScript(s, i); },
        /* 1 fennel */ [](const char* s, int i){ return fennel_engine::RunScript(s, i); },
#ifdef WF_ENABLE_WREN
        /* 2 wren   */ [](const char* s, int i){ return wren_engine::RunScript(s, i); },
#else
        /* 2 wren   */ nullptr,
#endif
#ifdef WF_ENABLE_FORTH
        /* 3 forth  */ [](const char* s, int i){ return forth_engine::RunScript(s, i); },
#else
        /* 3 forth  */ nullptr,
#endif
#if defined(WF_JS_ENGINE_QUICKJS) || defined(WF_JS_ENGINE_JERRYSCRIPT)
        /* 4 js     */ [](const char* s, int i){ return js_engine::RunScript(s, i); },
#else
        /* 4 js     */ nullptr,
#endif
#if defined(WF_WASM_ENGINE_WAMR) || defined(WF_WASM_ENGINE_WASM3)
        /* 5 wasm   */ [](const char* s, int i){ return wasm3_engine::RunScript(s, i); },
#else
        /* 5 wasm   */ nullptr,
#endif
    };
    RangeCheck(0, language, 6);
    AssertMsg(kDispatch[language] != nullptr, "no dispatch entry for language " << language);
    return Scalar::FromFloat(kDispatch[language](static_cast<const char*>(script), objectIndex));
}

this is actually incorrect in that neither lua nor fennel are required
fix the actual code
update the example

## 03:52:45

Scalar ScriptRouter::RunScript(const void* script, int objectIndex, int language)
{
    using RunFn = float(*)(const char*, int);
    static const RunFn kDispatch[6] = {
        /* 0 lua    */ [](const char* s, int i){ return lua_engine::RunScript(s, i); },
        /* 1 fennel */ [](const char* s, int i){ return fennel_engine::RunScript(s, i); },
#ifdef WF_ENABLE_WREN
        /* 2 wren   */ [](const char* s, int i){ return wren_engine::RunScript(s, i); },
#else
        /* 2 wren   */ nullptr,
#endif
#ifdef WF_ENABLE_FORTH
        /* 3 forth  */ [](const char* s, int i){ return forth_engine::RunScript(s, i); },
#else
        /* 3 forth  */ nullptr,
#endif
#if defined(WF_JS_ENGINE_QUICKJS) || defined(WF_JS_ENGINE_JERRYSCRIPT)
        /* 4 js     */ [](const char* s, int i){ return js_engine::RunScript(s, i); },
#else
        /* 4 js     */ nullptr,
#endif
#if defined(WF_WASM_ENGINE_WAMR) || defined(WF_WASM_ENGINE_WASM3)
        /* 5 wasm   */ [](const char* s, int i){ return wasm3_engine::RunScript(s, i); },
#else
        /* 5 wasm   */ nullptr,
#endif
    };
    RangeCheck(0, language, 6);
    AssertMsg(kDispatch[language] != nullptr, "no dispatch entry for language " << language);
    return Scalar::FromFloat(kDispatch[language](static_cast<const char*>(script), objectIndex));
}

this is soooo close to being able to collapse into 1 line (other languages can do it)
what options do we have to reduce the LOC for this? (ignore the #ifdef/#endif lines)

## 03:53:24

md

## 03:54:21

yup, that's whati thought, yay!!!! update code and example in doc; remove the lambda
we'll discuss theother possibilites after

## 03:57:26

Plans

put completed plans at end of the Plans section

## 03:59:01

review all .md files
look for justifiable uses of the sankey in the docs

## 04:01:05

WASM # sigil vs #!wasm\n — open question noted in wasm3 plan; tentatively keep # as discriminator, treat rest of first line as subtype.
Collapse #b64\n wasm sigil back to bare # — #b64\n is a workaround for cd.iff's ## TCL shell lines; revert once cd.iff is cleaned up. 

these are going away completely

## 04:03:00

i'd say  it works at least as well, if not better than before.
that said, i'm not really kept inside by the bush and floor geometry, but i don't think that's new, heh :/

## 04:03:11

md

## 04:04:10

both

## 04:08:54

30 +| 2026-04-16 | `95a065d` | 36,813   | −27,439   | −42.7%  | Jolt physics integration functional on snowgoons (`physics/jolt/`, +614 net) |                                                                  
note that we have NOT removed the existing wf physics (estimate it's LOC and project count if removed --- add that as a row below)

## 04:11:31

md

## 04:13:25

commit this

## 04:13:43

commit this

## 04:14:31

finish landing all the scripting languages, including testing in the game engine
update plans

## 04:14:36

finish landing all the scripting languages, including testing in the game engine
update plans
update docs

## 05:10:26

<task-notification>
<task-id>bsxxvqalr</task-id>
<tool-use-id>toolu_018PkXesTe9yrspziSvB7obA</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-WorldFoundry/0eefa1d6-49a4-4f61-a323-a656280fd3c7/tasks/bsxxvqalr.output</output-file>
<status>completed</status>
<summary>Background command "Build with Wren engine" completed (exit code 0)</summary>
</task-notification>

## 05:46:33

<task-notification>
<task-id>bo1gipomp</task-id>
<tool-use-id>toolu_01Q3zSt4WX3VcFafA9yYpygZ</tool-use-id>
<output-file>/tmp/claude-1000/-home-will-WorldFoundry/0eefa1d6-49a4-4f61-a323-a656280fd3c7/tasks/bo1gipomp.output</output-file>
<status>completed</status>
<summary>Background command "Rebuild wf_game with default engine config" completed (exit code 0)</summary>
</task-notification>

## 05:47:54

md scripting-languages.md

## 05:49:42

the summary on loc doc is out-of-date

## 05:51:22

updating scripting-languages.md:
under integration surface,
verify the binary cost and add a new column for ram footprint

## 05:55:34

md

## 06:05:11

JavaScript<br/>(QuickJS)
how can i break theline?

## 06:05:16

JavaScript<br/>(QuickJS)
how can i break the line?

## 06:06:08

i want you to move the integration surface section above reference scripts

## 06:08:36

the only meaningful thing left in the first tabl is notes, heh. add each language's note as a 2nd row below the language in the integration surface table

## 06:15:45

i don't know that i wanted you to trim any content from the table

## 06:15:57

probably better just to add to the 2nd row of each language?

## 06:17:03

we need to address levels and level assets, conversion to/from blender
we want to be able to edit the scripts in blender (and all the other things, too, ofc, heh)

## 06:18:00

2026-04-14     REST API box PoC     Deferred     Proof-of-concept for an HTTP API embedded in wf_game for creating/moving/deleting boxes at runtime. Not yet implemented.

we already did this one, btw

## 06:20:00

[Image #1]
i'm guessing the problem is that the 2nd row doesn't wrap?
this table is waaaay too wide. do not remove any information from it

## 06:25:12

wftools/vendor/{wasm3-v0.5.0,wamr-2.2.0}-wf/ — snowgoons WAT sources + compiled .wasm artifacts. 

i don't think we need to keep this (in doc or on disk ;-) )

## 06:34:44

we need to address levels and level assets, conversion to/from blender
we want to be able to edit the scripts in blender (and all the other things, too, ofc, heh)

evaluate how/if we can take the existing snowgoons.iff level and
1) import it into blender
2) make changes in blender to scripts (already done, but also need to execute plan 2026-04-16-script-language-oad-field
3) export it and run in the game engine

besides the actual conversion of assets, we need to check the orientation as they're loaded into blender and make sure they're with the correct orientation

## 06:41:19

i don't need all 3 columns the same width, just director and player; language can be much narrower

## 06:42:11

compile in only the language your scripts actually
->
compile in only the language(s) your scripts actually

## 06:42:35

we can just get rid of this completely:
Language dispatch is moving to OAD. Sigil sniffing is being replaced by an explicit ScriptLanguage field in the OAD (a dropmenu authored in Blender). RunScript will dispatch via a function-pointer table indexed by that field; the runtime will never guess. See docs/plans/2026-04-16-script-language-oad-field.md.

## 06:44:09

Binary cost and RAM figures are measured from the current WF build (x86-64, unstripped, debug)

with or without optimizations?

if debug unstripped, these numbers are practically meaningless. i'm sure you can calculate the stripped size and report them in the table

## 06:47:42

write plan to plans/
md

## 06:48:55

that's a separate discussion

## 06:49:21

update the doc with these examples

## 06:49:44

update status in wf-status.md

## 06:50:43

Lua 5.4.8     WF_ENABLE_LUA=1 (default on today)     ~224 KB     ~50 KB (lua_State + standard libs)

all languages are dfeault on today, heh

## 06:51:42

continue

## 07:00:10

md

## 07:01:55

ah, but i would like some compelling examples in fennel, too! ;-D

## 07:04:00

md

## 07:04:34

JavaScript
(JerryScript v3.0.0)     WF_JS_ENGINE=jerryscript     — (TODO measure)     <64 KB (configurable heap; IoT-targeted)

measure it , pls

## 07:11:52

return coroutine.create(function()
    while read_mailbox(INDEXOF_TRIGGER_A) == 0 do
        coroutine.yield()
    end
    write_mailbox(INDEXOF_CAMSHOT, 5)
    write_mailbox(INDEXOF_MUSIC, 2)
end)

how is this better than an if
let's discuss before making changes to doc

## 07:12:00

WebAssembly
(wasm3)     

;; INDEXOF_HARDWARE_JOYSTICK1_RAW=1009  INDEXOF_INPUT=3024
(module
  (import "env" "read_mailbox"  (func $read  (param i32) (result f32)))
  (import "env" "write_mailbox" (func $write (param i32 f32)))
  (func (export "main")
    i32.const 3024  i32.const 1009
    call $read  call $write))

    

(module
  (import "env" "read_mailbox"  (func $read  (param i32) (result f32)))
  (import "env" "write_mailbox" (func $write (param i32 f32)))
  (func $maybe (param $mb i32) (local $v f32)
    local.get $mb  call $read  local.tee $v
    f32.const 0  f32.ne
    if  i32.const 1021  local.get $v  call $write  end)
  (func (export "main")
    i32.const 100 call $maybe
    i32.const 99  call $maybe
    i32.const 98  call $maybe))
remove

## 07:12:49

explain these things in the doc
replace with meaningful improvement

## 07:13:56

remove that text

## 07:14:56

md

## 07:15:51

Wren and JS engines have the language-level primitives but would need the same per-tick resume logic added to their 
  RunScript implementations — that's a moderate but not large addition.

do it

update tables

update examples

## 07:26:19

in docs/coding-conventions.md:

**Better:**
```cpp
RangeCheck(0, language, number-of-items-in-array);
AssertMsg(kDispatch[language] != nullptr, "no dispatch table entry for language " << language);
```


how to calculate number of items in the language array?

## 07:26:45

update doc

## 07:27:20

why do weneed the cast to int? seems EXTRA

## 07:27:43

sometimes (a lot of the time, heh) i hate c++

## 07:28:27

let's fit it in the Range and Assert macros, eh?

## 07:29:42

let's fix it in the Range and Assert macros, eh?

## 07:32:05

try option B --- let's see what it looks like

## 07:32:51

 Cast all three operands to long long inside the comparison expressions. long long is signed, 64-bit everywhere we care about, and comfortably holds both
 int range and any practical size_t array size. No new headers needed.


that doesn't seem right. we don't even have long long on all platforms. it's just the signedness

## 07:36:33

yes

## 07:37:18

yes, step 5

## 07:37:33

sure

## 07:39:08

[Image #1]
what's going on here with md-to-pdf.sh ?

## 07:39:33

commit this

## 07:39:40

update plan

## 07:42:45

commit

## 07:42:59

JavaScript
(JerryScript v3.0.0)     WF_JS_ENGINE=jerryscript     — (TODO measure)     <64 KB (configurable heap; IoT-targeted)

how's this measurement going?

## 07:45:59

cd.iff packing

After levcomp-rs produces a binary level .iff, inject it into cd.iff. Either extend iffcomp-rs or add a small cdpack-rs tool to replace a named level slot. Secondary concern — tackle after levcomp-rs works on a test file.

NO
we'll just rebuild from cd.iff.txt --- that's very much one of our goals

## 07:46:32

you should go ahead and extra the other levels, too. you're very good at chunk manipulation :)

## 07:46:52

you should go ahead and extract the other levels, too (and essentially vendor them at this point). you're very good at chunk manipulation :)

## 07:48:21

33 +| JavaScript<br/>(JerryScript v3.0.0) | `WF_JS_ENGINE=jerryscript` | ~237 KB (wf-minimal profile) | <64 KB (configurable heap; IoT-targeted) |                                    
and that's with -O2, you say? :O

## 07:49:15

ok thx

## 07:49:18

md

## 07:52:04

ok, now write some forth to create a coroutine in syntax and function

## 07:52:45

ok, now write some forth to create a coroutine in syntax and in actual function

## 08:06:32

md

## 08:06:40

yes, after md

## 08:07:17

md

## 08:09:47

[Image #1]
can you start the table earlier? in it's "natural" place---don't require tables to start at the top of a page if they won't fit on the remainder of the current page

## 08:09:58

[Image #1]
can you start the table earlier? in it's "natural" place---don't require tables to start at the top of a page if they won't fit on the remainder of the current page
md-to-pdf.sh

## 08:11:31

md

## 08:15:55

commit this

## 08:16:43

sure, gap 4

## 08:24:00

continue

## 09:08:52

what next?

## 09:11:27

2026-04-16-script-language-oad-field.md

## 09:14:46

i would say that wftools/wf_engine belongs in wfsource/source
what's  your opinion?

## 09:17:02

well, maybe we do need an additional directory, as well, but the is, wf_engine is not supposed to be some sort of build haress, but rather THE ACTUAL GAME EXECUTABLE!

## 09:18:25

#2 and you're right about most if not all downloads vendored in the wrong place, as well

## 09:19:55

next

## 09:21:18

continue

## 09:22:13

md

## 09:24:25

oh, that was already done. go ahead
keeping in mind there's the new scripting language field and that might affect some sizes

## 09:26:21

1. keep in docs/plans/
2. keep what they are now (for now)

## 09:31:12

close
update plan
update docs
commit

## 09:32:23

md plan

## 09:32:53

write

## 09:34:36

what's left on 2026-04-14-forth-scripting-engine.md  ?

## 09:34:49

add to plan to update scripting-languages.md with number

## 09:35:14

the memory stats

## 09:35:54

go

## 09:36:33

ok do it

## 09:36:58

they were mostly deleted files, right?

## 09:37:21

1

## 09:37:59

md plan

## 09:38:58

do it
