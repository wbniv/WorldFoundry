# Test image for foundry-linux-setup on Ubuntu 26.04 (target LTS)
# Usage:
#   docker build -f test/Dockerfile.ubuntu26.04 -t foundry-test:26.04 .
#   docker run --rm foundry-test:26.04 --dry-run
FROM ubuntu:26.04

RUN apt-get update -q && apt-get install -y --no-install-recommends \
        bash sudo curl ca-certificates lsb-release \
    && rm -rf /var/lib/apt/lists/*

RUN useradd -m -s /bin/bash -G sudo tester \
    && echo 'tester ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers
USER tester
WORKDIR /home/tester

COPY --chown=tester:tester install.sh /home/tester/install.sh
RUN chmod +x /home/tester/install.sh

ENTRYPOINT ["bash", "/home/tester/install.sh"]
CMD ["--help"]
