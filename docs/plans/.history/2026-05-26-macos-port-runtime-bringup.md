| Date | Change |
|------|--------|
| [2026-05-26](https://github.com/wbniv/WorldFoundry/commit/92b325a2) | feat(macos): .app bundle + NSBundle asset accessor |
| [2026-05-26](https://github.com/wbniv/WorldFoundry/commit/bd73058a) | feat(macos): renderer-agnostic headless runtime bring-up |

<!--history-meta v1
92b325a2	author	Will Norris
92b325a2	added	13
92b325a2	deleted	1
92b325a2	files	1
92b325a2	body	Extends the headless bring-up (bd73058a) with proper macOS .app packaging,\nauthored before the first Codemagic run so both phases land together:\n\n- macos/Info.plist — macOS desktop bundle plist (LSMinimumSystemVersion=12.0,\n  NSHighResolutionCapable, no iOS-specific keys).\n- hal/macos/asset_accessor_nsbundle.mm — NSBundle accessor (clone of\n  hal/ios/asset_accessor_nsbundle.mm; Foundation/NSBundle API is identical on\n  macOS and iOS).\n- hal/linux/platform_init.cc — WF_TARGET_MACOS guard selects\n  HALCreateNSBundleAccessor() over HALCreatePosixAssetAccessor(); Linux\n  unchanged. asset_accessor_posix.cc removed from macOS CMake sources.\n- CMakeLists.txt — macOS add_executable gains MACOSX_BUNDLE; new macOS bundle\n  properties block (bundle ID, 12.0 deployment target); resource-copy loop\n  bundles cd.iff/level0.mid/soundfont into Contents/Resources/; NSBundle .mm\n  replaces posix accessor in macOS stub-sources list.\n- codemagic.yaml — smoke-test binary path updated to\n  wf_game.app/Contents/MacOS/wf_game; comment notes -L bypasses the\n  AssetAccessor so the NSBundle lookup for cd.iff is never exercised in CI.\n\nLinux build verified green (platform_init.cc compiles with the new guard;\nasset_accessor_posix.cc still compiled on Linux via the #else branch).\n\nPlan: docs/plans/2026-05-26-macos-port-runtime-bringup.md (Phase 2 section)\n\nCo-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
bd73058a	author	Will Norris
bd73058a	added	46
bd73058a	deleted	0
bd73058a	files	1
bd73058a	body	Brings macOS desktop to ~iOS-current parity WITHOUT graphics: wf_game\ncompiles + links under Apple Clang and runs the engine Load/Step/Unload\nloop headless (rendering is a TRUE no-op — no window, no Metal, no GL\ncontext). macOS boots 'Linux-minus-X11': main() -> HALStart() on the\nmain thread, reusing the portable Linux HAL (platform_init/audio/input/\nlifecycle/asset_accessor_posix) verbatim.\n\nNew hal/macos/: platform_main (shell), display_macos (headless Display,\nclone of display_ios), gl_stubs (no-op GL entry points), window_macos\n(HAL window-close trio that mesa.cc provides on Linux). New\nengine/stubs/renderer_stub.cc = no-op RendererBackend.\n\nAll shared-file edits are WF_TARGET_MACOS-guarded (backend_factory,\ndisplay.cc, renderer.hp GL-header arm, wfprim.h GLU guard,\nrenderpoly3d_stubs, pigsys.hp cf-header). CMakeLists gains an\n'APPLE AND NOT IOS' arm across every platform block; macOS joins the\nminimal-config gate (Forth-only) and is excluded from the X11/GLX test\ntargets. New macos-desktop-debug Codemagic workflow (Ninja, not Xcode,\nso Jolt stays modifiable; first build forces legacy physics + ASan-off\nto fit the Mac-min budget).\n\nVerified locally (free): Linux build green, cmake configure parses,\ncodemagic.yaml valid. macOS-only files compile-test on Codemagic.\n\nPlan: docs/plans/2026-05-26-macos-port-runtime-bringup.md\nInvestigation: docs/investigations/2026-05-26-macos-port-estimate.md\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
-->
