| Date | Change |
|------|--------|
| [2026-09-19](https://github.com/wbniv/WorldFoundry/commit/c3ef550b) | feat(condo): site skybox from the real location, units on the 6th floor, balcony POV pan |

<!--history-meta v1
c3ef550b	author	Will Norris
c3ef550b	added	381
c3ef550b	deleted	0
c3ef550b	files	1
c3ef550b	body	The condo level now sits where the condo is: 34 Soi Sathu Pradit, Bang Phong Phang\n(13.6935894, 100.5334137), back/balconies facing west. site_constants.py fixes the\ncompass (+X north, +Y west), the pin and the elevation — UNIT_Z = 5 × (3.0 + 0.15 m\nslab) = 15.75 m — shared by the level script and make_site_textures.py, which turns a\ncommitted OpenStreetMap extract (site-osm.json, ODbL) into two 24-bit TGAs offline and\nbyte-stably: condo_ground.tga (512², ±80 m flat map, the doll-house camera's ground) and\ncondo_sky.tga (1024×512 equirect: gradient, sun glow at the light's effective azimuth,\nevery building as a wall/roof band at its true azimuth and elevation from 15.75 m, the\nground palette projected below the horizon). Both ride a 1024² PERM page\n(textile.flags + the --vram-* flags in run-condo and the recorder).\n\nsite-ground and its boolean-cut machinery are gone: a textured quad at world z = 0, an\ninverted 160 m sky dome at the viewpoint, and an open-air corridor + parapet outside\nthe front doors replace it. Everything is authored with the unit floor at z = 0 and\nlifted in one pass at the end, so the tour path, darken_floors and the slab outline\nnever see the offset. Spawns (level and tour) move just inside 639's front door.\n\nBalcony camera: two ActBoxOR zones + a Director multiplexer switch to cs_balcony — the\nplayer's POV, a Relative (0, 1.5, 1.7) offset that lands just past the pony wall — and a\nscripted look target pans 5 s from the street to the south-west up to the towers to the\nnorth-west; walking back inside restores the doll-house. The tour's patio waypoints\nhold 3 s each (new per-waypoint `hold`) and the cut is 35 s.\n\nThings learned the hard way, all recorded in the plan and the level README:\n- WF texture V is top-down (material.cc CalcVRAMuv, exporter passes uv.y through) —\n  the first sky had the ground at the zenith.\n- CalcVRAMuv bounds every texture's u by the *transient* slot width, PERM included, so\n  --vram-slot-* must widen too.\n- The camera is a physics body that climbs whenever its bbox overlaps a Mass > 0 actor;\n  the dome's bbox is the whole level, so it gets Mass 0. That, not the CamShot toggles,\n  is why every shot placed inside the balcony ended up 30 m in the air.\n- The Sun's altitude never reached the renderer (light.hpi rotates local +X); the sky\n  is painted at the effective azimuth.\n- -record_video stamped frames at a fixed 30 fps while captions ran on the level\n  (wall) clock: at ~23 fps the video played 1.3× fast and captions lagged up to 8 s.\n  display.cc now stamps frames with the wall clock and resamples to cfr 30.\n- The condo-level task's `| grep … || true` masked Blender failures and shipped the\n  previous .lev; it no longer does.\n\nPlan: docs/plans/2026-09-19-condo-site-skybox.md\n\nCo-Authored-By: Claude Opus 5 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_017hM4VFRoFFkipNVRLTs42g
-->
