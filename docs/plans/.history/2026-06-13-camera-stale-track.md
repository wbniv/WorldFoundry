| Date | Change |
|------|--------|
| [2026-06-13](https://github.com/wbniv/WorldFoundry/commit/16e07a8b) | fix(engine): camera degrades gracefully on a stale/destroyed track index (closes TODO 134+135) |

<!--history-meta v1
16e07a8b	author	Will Norris
16e07a8b	added	86
16e07a8b	deleted	0
16e07a8b	files	1
16e07a8b	body	Two TODO bugs were the same bug: the camera resolves a tracked-actor INDEX\n(cd.idxTrackObject) and asserts/UAFs when it's invalid. 134's "dt-spike → garbage\nactor lookup" framing was a misdiagnosis — deltaTime is clamped to <=100ms at three\nlayers (MeasureDelta, StepFrame, and Level::update itself since the 2010 first\ncommit) and RunLevel routes through StepFrame, so dt never reaches actor movement;\nthe idxObject=31158 assert came from the stale camera index. 135 is the same index\nread on the despawn path (movecam.cc GetWatchObject / SetCameraParametersFromShot).\n\nFix: a shared non-asserting CameraHandler::ResolveTrackObject(int32) returns the\ntracked PhysicalObject* or null — it bounds-checks against GetMaxObjectIndex()\nBEFORE Level::GetObject's RangeCheck (the OOR index is what aborts), and rejects\nempty/freed slots and non-physical objects.\n- The three GetWatchObject handlers (Normal/Pan/Bungee) collapse to\n  `return ResolveTrackObject(cd.idxTrackObject);` — the mainCharacter() fallback is\n  DELETED (per the no-main-character-special-casing decision); the sole caller\n  level.cc:949 already null-skips, so null degrades to "active rooms hold this frame."\n- SetCameraParametersFromShot (the real per-frame crash) resolves via\n  ResolveTrackObject; if the track is gone it drops the index (->0) and uses a zero\n  track offset + skips rotation-tracking, so the camera degrades to its\n  Follow-relative shot position. Always a valid destCam → no caller rewiring.\n\nKnown limit: an index isn't an identity and AddObject reuses freed slots, so a\nRECYCLED slot still aliases the new occupant — filed as an actor-identity /\ngeneration-ID follow-up TODO, referenced in the code comment.\n\nRegression guard wfmut_smoke C1-C6 (CameraHandler::ResolveTrackObject): unset(0),\nnegative, the reported OOR index 31158, at-bound, a valid player actor, and an empty\nslot. Verified: with the bounds-check temporarily removed the smoke ABORTS at C3 with\nthe exact reported assert (idxObject = 31158, _actors.Size() = 331, level.cc:1752);\nrestored, C1-C6 all pass. (The unrelated SR0 spawn-test failure is pre-existing.)\nCorrected the dt-mis-blame comments in verify_wfmut_bridge.py / verify_smb_w1_3_enemies.py.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
